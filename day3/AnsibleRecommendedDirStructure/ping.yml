- name: First Play
  hosts: ubuntu 
  gather_facts: no
  tasks: 
    - name: Ping the ansible node
      ping:

- name: Second Play
  hosts: centos
  gather_facts: no
  tasks: 
    - name: Ping the ansible node
      ping:
